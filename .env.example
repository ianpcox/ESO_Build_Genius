# Copy to .env and set your local paths. Used by ingest and refresh_addon_data.py.
# Do not commit .env.

# ESO game install (for EsoExtractData / raw client data)
ESO_INSTALL_PATH=Z:\The Elder Scrolls Online

# ESO live folder (AddOns, SavedVariables - addon export output)
ESO_LIVE_PATH=C:\Users\Ian\OneDrive\Documents\Elder Scrolls Online\live

# Optional: only if a service requires auth
# ESO_USERNAME=
# ESO_PASSWORD=
